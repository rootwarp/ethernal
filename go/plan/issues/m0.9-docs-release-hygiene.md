# Phase M0.9 — Documentation & Release Hygiene (v0.2 Docs)

**Objective.** Update operator-facing documentation to match the binary; produce the v0.2 CHANGELOG.md and MIGRATION.md required by FR-P0-F2. After this phase, docs and code are in sync at the v0.2 boundary.

**Dependencies.** M0.4 (new flag is the headline breaking change), M0.5–M0.8 (every M0 phase changes CLI surface or behavior).

**Exit criteria recap (project-plan §M0.9).**
- USER-GUIDE walked end-to-end by maintainer with fresh checkout produces a successful hoodi deposit run.
- CHANGELOG.md has v0.2.0 entry covering every breaking change.
- MIGRATION.md v0.1 → v0.2 published with concrete before/after flag examples.
- `grep -r 'staking-deposit-cli' docs/` returns no hits.

**Phase totals.** 5 issues, 8 points (~4 days single-stream).

---

## Issue M0.9-1 — USER-GUIDE.md `--withdrawal-address` example + troubleshooting refresh

- **Points:** 2
- **Priority:** P0
- **Closes:** FR-P0-F1 (GO-052)
- **Depends on:** M0.4-2

**Description.** Update `docs/USER-GUIDE.md` to show the `--withdrawal-address` example producing a real `0x01` credential. Remove every reference to the v0.1 placeholder behavior. Refresh troubleshooting rows that reference renamed/relocated sentinels.

**Implementation notes.**
- File: `docs/USER-GUIDE.md` — `docs/USER-GUIDE.md:217` (per REVIEW GO-052) is the headline section.
- Worked example: hoodi deposit using a checksummed EIP-55 address; show derived 0x01 credential format.
- Filename scheme example: `deposit_data-<RFC3339Nano>-<sha256[:4]>.json` per architecture §10.2.
- Concrete fixture bytes will get a follow-up edit after M0.10 (per project-plan Risk P10); this issue uses placeholder patterns labeled "example".

**Acceptance criteria.**
- [ ] Section shows the `--withdrawal-address` flag with EIP-55 example.
- [ ] No reference to `defaultWithdrawalCreds` / placeholder behavior remains.
- [ ] Troubleshooting rows match current sentinel names (cross-check with architecture §15 table).
- [ ] Maintainer walks the guide end-to-end against hoodi and reports success (manual check; record in PR description).

---

## Issue M0.9-2 — CHANGELOG.md v0.2.0 entry

- **Points:** 2
- **Priority:** P0
- **Closes:** FR-P0-F2 (CHANGELOG portion)
- **Depends on:** M0.4–M0.8 substantially complete

**Description.** Add a v0.2.0 entry to `CHANGELOG.md` documenting every breaking change:
- New required flag: `--withdrawal-address` on `eth-deposit-gen`.
- New required flags: `--nonce` + fee flags on build/run.
- `--rpc-url` rejected on build/run.
- JSON schema break: `UnsignedTx.From` field removed.
- Validation tightening: SSZ root recompute, network/fork binding, BLS verify on read path, `0x00` all-zero rejection.
- Exit-code unification across build/sign/run/send.
- Filename scheme change for deposit_data files.

**Implementation notes.**
- Follow Keep-a-Changelog format (existing `CHANGELOG.md` convention).
- Each bullet links to the GO/FR ID closing it.
- Include "Migration: see MIGRATION.md".

**Acceptance criteria.**
- [ ] CHANGELOG.md v0.2.0 entry present.
- [ ] Every M0 PR's breaking change is referenced.
- [ ] CHANGELOG entry passes a manual checklist against PRD §8.3 (breaking changes section).

---

## Issue M0.9-3 — MIGRATION.md v0.1 → v0.2

- **Points:** 2
- **Priority:** P0
- **Closes:** FR-P0-F2 (MIGRATION portion)
- **Depends on:** M0.9-2

**Description.** Create `MIGRATION.md` with worked before/after CLI examples for every v0.2 break. Mitigation R10 (project-plan Risk Register): the `--rpc-url` rejection error message points at the new `--nonce`/fees flow; MIGRATION must repeat the concrete example.

**Implementation notes.**
- Section per break:
  1. `--withdrawal-address` required → show before/after for hoodi + mainnet.
  2. `--nonce` + fee flags required on build/run → before/after.
  3. `--rpc-url` rejected on build/run → before/after; reference ADR-004.
  4. JSON schema break (`UnsignedTx.From`) → JSON before/after.
  5. SSZ-root/network-binding tightening → operator-visible reject messages.
  6. Exit code 5 on receipt revert/timeout (was 0) — automation update.
- File location: `MIGRATION.md` in repo root.

**Acceptance criteria.**
- [ ] MIGRATION.md exists with all 6 sections above.
- [ ] Every section has a runnable before/after example.
- [ ] ADR-004 link present in `--rpc-url` section.
- [ ] Document referenced from CHANGELOG.md v0.2.0 entry.

---

## Issue M0.9-4 — `cmd/eth-deposit-tx/main.go:30-38` version comment block

- **Points:** 1
- **Priority:** P0
- **Closes:** Architecture §19 conflict #7
- **Depends on:** M0.9-2

**Description.** Update the version comment block at `cmd/eth-deposit-tx/main.go:30-38` to reflect v0.2's breaking nature ("first feature-complete release; previous v0.1 ships an all-zero placeholder WC and must not be used").

**Implementation notes.**
- File: `cmd/eth-deposit-tx/main.go:30-38`.
- Mirror the CHANGELOG headline.
- Also audit `cmd/eth-deposit-gen/main.go:29` (CLIVersion) — although that's part of M0.9-5 staking→ethstaker rename, the version-comment update can be folded in or split.

**Acceptance criteria.**
- [ ] Comment block updated.
- [ ] `--version` output on both binaries reports `v0.2.0` after M0.11 tag (forward-compat check; the comment is the prose, the version string lands at M0.11-3 tag).

---

## Issue M0.9-5 — Rename `staking-deposit-cli` → `ethstaker-deposit-cli` in docs

- **Points:** 1
- **Priority:** P0
- **Closes:** Architecture §19 conflict #1 (research/01 §R2)
- **Depends on:** —

**Description.** Replace every `staking-deposit-cli` reference in `docs/`, `cmd/eth-deposit-gen/main.go:29` (CLIVersion comment), `:56-59` (error doc strings), `:144-154` (`runDepositCLIVerify` source-of-authority docs). The actual subprocess wiring and sanitization land in M0 / M1 (M1.7-1 ships the hermetic CI image).

**Implementation notes.**
- Targets per architecture §19 conflict #1:
  - `cmd/eth-deposit-gen/main.go:29, :56-59, :144-154`.
  - `docs/USER-GUIDE.md` references.
  - Any README / inline comments.
- Do NOT change the actual command invoked yet if the binary on PATH is still named `staking-deposit-cli` for some operators — document the rename in MIGRATION.md (M0.9-3 cross-issue).

**Acceptance criteria.**
- [ ] `grep -r 'staking-deposit-cli' docs/ cmd/` returns no hits.
- [ ] All references now read `ethstaker-deposit-cli`.
- [ ] MIGRATION.md notes the rename for operators who scripted against the older name.
