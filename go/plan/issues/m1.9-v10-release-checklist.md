# Phase M1.9 — v1.0 Release Checklist & Tag

**Objective.** Execute the M1 release checklist (PRD §12 exit criteria); produce a tagged v1.0.0 release.

**Dependencies.** M1.1–M1.8 all complete.

**Exit criteria recap (PRD §12 M1 exit).**
- All P1 acceptance tests green.
- `make test-cross-validate` passing in CI for every supported network.
- Mainnet ack gate integration test green.
- Mainnet dry-run completed without warning; outcome recorded.
- Tagged `v1.0.0`.

**Phase totals.** 5 issues, 7 points (~3.5 days single-stream + dry-run coordination).

---

## Issue M1.9-1 — `make test-cross-validate` green for every supported network

- **Points:** 1
- **Priority:** P1
- **Closes:** PRD §12 M1 exit criterion
- **Depends on:** M1.7-3

**Description.** Run `make test-cross-validate` in CI for hoodi and mainnet; record green status in release notes.

**Implementation notes.**
- The job already exists from M1.7-3; this issue is a confirmation gate before tagging.
- If the image pin needs a bump, do it here.

**Acceptance criteria.**
- [x] CI run on `main` HEAD shows `make test-cross-validate` green for hoodi and mainnet.

---

## Issue M1.9-2 — Mainnet ack gate integration test green

- **Points:** 1
- **Priority:** P1
- **Closes:** PRD §12 M1 exit criterion
- **Depends on:** M1.6-4

**Description.** Run the `mainnet_gate_test.go` matrix from M1.6-4 in CI; confirm all rows pass.

**Implementation notes.**
- Already part of `make test` post-M1.6; this is a confirmation step.

**Acceptance criteria.**
- [ ] `go test -run TestMainnetGate ./cmd/eth-deposit-tx/...` green.
- [ ] CI on `main` HEAD shows matrix green.

---

## Issue M1.9-3 — Maintainer-led mainnet dry-run + record outcome (Spike S6)

- **Points:** 2
- **Priority:** P1
- **Closes:** PRD §12 M1 exit criterion ("mainnet dry-run completed without warning")
- **Depends on:** M1.6-4

**Description.** Per project-plan Spike S6: a maintainer runs a dry-run mainnet ceremony on a held-out test wallet (or a recorded `dryrun` mode if available). Record outcome (warnings, prompts, exit codes) in release notes.

**Implementation notes.**
- "Held-out test wallet" preferred per PRD §12 M1 exit criterion.
- If a `--dryrun` mode is added in M1.6, document it here.
- Outcome recorded in `docs/RELEASE-NOTES-v1.0.md` (or GitHub release body): wallet used, network, prompt text, on-screen warnings, exit code, tx hash if broadcast.

**Acceptance criteria.**
- [ ] Dry-run executed; outcome recorded.
- [ ] No warning surfaces unrelated to expected mainnet-gate prompts.
- [ ] Sign-off from a second maintainer (cross-check).

---

## Issue M1.9-4 — Final `make lint` clean + tag v1.0.0 + binaries

- **Points:** 2
- **Priority:** P1
- **Closes:** The release tag
- **Depends on:** M1.9-1, M1.9-2, M1.9-3

**Description.** `make lint` clean (gofmt, vet, staticcheck, errcheck, govulncheck — no new reachable hits per FR-P0-E2 policy). Tag `v1.0.0` on `main`. Cut binaries for linux-amd64, darwin-arm64, darwin-amd64.

**Implementation notes.**
- Same pattern as M0.11-3 but on the v1.0 commit.
- Confirm CHANGELOG v1.0.0 (M1.8-2) and any v1.0 USER-GUIDE additions (M1.8-3) are on the tagged commit.

**Acceptance criteria.**
- [ ] `make lint` exits zero.
- [ ] Tag `v1.0.0` on `main`.
- [ ] 3 binaries attached to GitHub release.

---

## Issue M1.9-5 — Release notes including dry-run outcome

- **Points:** 1
- **Priority:** P1
- **Closes:** v1.0 release process
- **Depends on:** M1.9-3, M1.9-4

**Description.** Publish v1.0.0 release notes referencing the M1.9-3 dry-run outcome, the M1.8 docs, and the CHANGELOG.

**Implementation notes.**
- File: `docs/RELEASE-NOTES-v1.0.md` plus GitHub release body.
- Highlight the mainnet ack gate as the headline v1.0 feature.

**Acceptance criteria.**
- [ ] Release notes published.
- [ ] Cross-links to CHANGELOG, USER-GUIDE mainnet section, dry-run outcome.
