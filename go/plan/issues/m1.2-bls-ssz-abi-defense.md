# Phase M1.2 — BLS / SSZ / ABI Correctness Defense-in-Depth

**Objective.** Add external-authority cross-checks (differential SSZ oracle, `accounts/abi` cross-check) and the BLS scalar/identity rejections promised in FR-P1-C.

**Dependencies.** M0.11.

**Exit criteria recap (project-plan §M1.2).**
- `go test ./internal/ssz/...` (default tags) passes; oracle tests skipped without tag.
- `go test -tags=differential_oracle ./internal/ssz/...` passes (fastssz vs our impl agree on the fuzz corpus).
- `TestPackDeposit_AgainstGethABI` passes; fuzz lane stable for 60s.
- `bls.NewSigner` rejects 32-byte zero secret → exit 3.
- `bls.ValidatePubkeyBytes` rejects compressed-G1 infinity (`0xc0` + 47 zero bytes).
- `network.DomainDeposit()` and `ZeroGenesisValidatorsRoot()` are functions.

**Phase totals.** 7 issues, 13 points (~6.5 days single-stream).

---

## Issue M1.2-1 — `bls.NewSigner` rejects zero scalar (`ErrSecretZero`)

- **Points:** 1
- **Priority:** P1
- **Closes:** FR-P1-C1 (GO-036)
- **Depends on:** —

**Description.** After `bls.SecretKey.Deserialize` succeeds in `internal/bls/bls.go:69-92`, reject `s.sk.IsZero()` with new sentinel `ErrSecretZero` (research/03 §2).

**Implementation notes.**
- New sentinel: `var ErrSecretZero = errors.New("bls: secret key is zero")` per architecture §15.
- Exit code 3 per architecture §15 table.
- Alternative implementation per PRD: use `GetSafePublicKey()` — but the IsZero check is cleaner and explicit.

**Acceptance criteria.**
- [x] `TestNewSigner_ZeroScalar_Reject`: feed 32-byte zero secret → `ErrSecretZero`.
- [x] Existing happy-path tests green.

---

## Issue M1.2-2 — `ValidatePubkeyBytes` rejects point-at-infinity (`ErrPubkeyZero`)

- **Points:** 1
- **Priority:** P1
- **Closes:** FR-P1-C2 (GO-037)
- **Depends on:** —

**Description.** After successful Deserialize in `internal/bls/bls.go:154-165`, reject `hPub.IsZero()` with new sentinel `ErrPubkeyZero` matching IETF `KeyValidate` (research/03 §3).

**Implementation notes.**
- New sentinel: `var ErrPubkeyZero = errors.New("bls: pubkey is point at infinity (KeyValidate rejected)")` per architecture §15.
- The compressed-G1 infinity encoding is `0xc0` followed by 47 zero bytes.
- Exit code 2 (consumed via wrapped sentinel in `Entry.ValidateForNetwork`).

**Acceptance criteria.**
- [ ] `TestValidatePubkeyBytes_Identity_Reject`: feed `0xc0` + 47 zero bytes → `ErrPubkeyZero`.
- [ ] `TestValidatePubkeyBytes_OnCurve_Accept`: real G1 point from a fixture → nil.

---

## Issue M1.2-3 — `DomainDeposit` + `ZeroGenesisValidatorsRoot` → functions

- **Points:** 2
- **Priority:** P1
- **Closes:** FR-P1-C3 (GO-038)
- **Depends on:** —

**Description.** Convert `internal/network.DomainDeposit` and `ZeroGenesisValidatorsRoot` from exported package vars to functions returning the array by value, backed by unexported values per architecture §6.1 and §15.

**Implementation notes.**
- File: `internal/network/network.go`.
- ```go
  func DomainDeposit() [4]byte
  func ZeroGenesisValidatorsRoot() [32]byte
  ```
- All callers must update; `internal/deposit.NewGenerator` is the main consumer per PRD §6.2.3 FR-P1-C3.
- Source values become unexported package-level constants/vars.
- Audit `grep -rn "DomainDeposit\|ZeroGenesisValidatorsRoot"` for callers; update each.

**Acceptance criteria.**
- [ ] Both symbols are functions; original vars deleted.
- [ ] `grep -rn "network.DomainDeposit[^(]" .` returns no hits (anything not followed by `(` is broken).
- [ ] Compile clean; all tests green.

---

## Issue M1.2-4 — Differential SSZ oracle: sszgen scaffold + committed generated + tests (split-watch)

- **Points:** 4
- **Priority:** P1
- **Closes:** FR-P1-C4 (GO-048), ADR-007
- **Depends on:** —

**Description.** Per architecture §11.1 and ADR-007: hand-write minimal `DepositMessage`/`DepositData` Go structs in `internal/ssz/testdata/oracle_types.go`; run `sszgen` once and **commit** the generated `oracle_types_ssz.go`. Add `TestDifferentialDepositMessageRoot` and `TestDifferentialDepositDataRoot` behind `//go:build differential_oracle` that drive both implementations from a shared seed-anchored fuzz corpus.

**Implementation notes.**
- File: new `internal/ssz/testdata/oracle_types.go`, `internal/ssz/testdata/oracle_types_ssz.go` (generated, committed), `internal/ssz/ssz_oracle_test.go` (test).
- Build tag: `//go:build differential_oracle`.
- Add `github.com/ferranbt/fastssz/sszgen` to `tools/tools.go` under `tools` tag per architecture §12.2 (extension of M0.1-3).
- Test-only dep: `github.com/ferranbt/fastssz` added to `go.mod` (it's gated by the build tag; `go.mod` carries it).
- **Split-watch:** if sszgen integration is fiddly or the generated file is large, split into "sszgen scaffold + committed types" (2 pt) + "oracle tests + fuzz corpus" (2 pt).

**Acceptance criteria.**
- [ ] `go generate ./...` (or `make oracle-regen`) regenerates the file; CI verifies it's up to date.
- [ ] `go test -tags=differential_oracle ./internal/ssz/...` passes; both implementations agree on the seeded fuzz corpus.
- [ ] Default `go test ./internal/ssz/...` skips the oracle tests (verified by `-v` output).

---

## Issue M1.2-5 — CI lane for `differential_oracle`

- **Points:** 1
- **Priority:** P1
- **Closes:** Required for FR-P1-C4 release-gate
- **Depends on:** M1.2-4

**Description.** Add `.github/workflows/differential-oracle.yml` running `go test -tags=differential_oracle ./internal/ssz/...` on every PR/push.

**Implementation notes.**
- Reuse the existing test-job pattern; pin toolchain per M0.1-2.
- Cache `fastssz` module download.

**Acceptance criteria.**
- [ ] CI workflow `differential-oracle` exists and is required for merge.
- [ ] Green on a fresh PR; visible failure on a deliberately-introduced SSZ bug (verified).

---

## Issue M1.2-6 — `PackDeposit` `accounts/abi` cross-check + fuzz

- **Points:** 3
- **Priority:** P1
- **Closes:** FR-P1-C5 (GO-070)
- **Depends on:** M0.2-1

**Description.** Per architecture §11.2: add `TestPackDeposit_AgainstGethABI` to `internal/tx/abi_diff_test.go`. Independently encode the same args via `accounts/abi.Pack`; assert byte equality with `PackDeposit`. Drive by a small fuzz target so layout edge cases hit both encoders.

**Implementation notes.**
- File: new `internal/tx/abi_diff_test.go`.
- No build tag needed — `accounts/abi` is already a transitive geth dep per architecture §11.2.
- Embed the deposit ABI JSON string at the top of the test file (the `deposit(bytes,bytes,bytes,bytes32)` signature).
- Fuzz target runs ≥ 60 s in the CI fuzz lane.

**Acceptance criteria.**
- [ ] `TestPackDeposit_AgainstGethABI` (single concrete vector) green.
- [ ] `FuzzPackDeposit_AgainstGethABI` stable for 60 s (no diverging inputs).
- [ ] Test discoverable via `go test -run TestPackDeposit_AgainstGethABI ./internal/tx/...`.

---

## Issue M1.2-7 — Delete dead oracle stubs + tautological fuzz assertions

- **Points:** 1
- **Priority:** P1
- **Closes:** FR-P1-C4 cleanup
- **Depends on:** M1.2-4

**Description.** Delete dead `computeDepositMessageRoot`/`computeDepositDataRoot` stubs in `internal/ssz/ssz_test.go:333-448` and the tautological `FuzzMerkleize`/`FuzzUint64Chunk` assertions in `internal/ssz/ssz_fuzz_test.go:50-91` (architecture §11.1 last paragraph).

**Implementation notes.**
- Net negative line count; ensure no test that meaningfully exercises behavior is removed (only the tautological self-assertions).
- Document in commit message that the differential oracle (M1.2-4) replaces the assertion role.

**Acceptance criteria.**
- [ ] `grep -rn "computeDepositMessageRoot\|computeDepositDataRoot" internal/ssz/` returns no hits.
- [ ] Removed fuzz assertions documented in CHANGELOG (M1.8-2 cross-issue).
- [ ] `go test ./internal/ssz/...` still green (the differential lane covers what was lost).
