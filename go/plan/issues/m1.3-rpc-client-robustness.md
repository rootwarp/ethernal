# Phase M1.3 — RPC Client Robustness

**Objective.** Close FR-P1-D — receipt polling, fee resolution, and the hybrid `--rpc-url` decision (ADR-004 lock-in) become safe.

**Dependencies.** M0.11.

**Exit criteria recap (project-plan §M1.3).**
- Mock RPC: `HeaderByNumber` with `BaseFee == nil` → `tx.ErrNoBaseFee`.
- Mock RPC: `chainID == 0` → fail closed (exit 5).
- Mock RPC: `NotFound` after deadline → `tx.ErrReceiptTimeout`.
- Gas overflow regression: `estimate = math.MaxUint64 - 1024` → no overflow.
- `eth-deposit-tx run --rpc-url URL` (no `--nonce`/fees) succeeds against hoodi end-to-end; `build --rpc-url URL` still rejected.

**Phase totals.** 6 issues, 13 points (~6.5 days single-stream).

---

## Issue M1.3-1 — `BlockBaseFee` → `HeaderByNumber` + `ErrNoBaseFee`

- **Points:** 2
- **Priority:** P1
- **Closes:** FR-P1-D1 (GO-032)
- **Depends on:** —

**Description.** Replace `BlockByNumber` with `HeaderByNumber(ctx, nil)` in `internal/tx/rpc_client.go:120-126`. Return new sentinel `tx.ErrNoBaseFee` when `header.BaseFee == nil`. Fix interface doc to say "latest" not "pending".

**Implementation notes.**
- File: `internal/tx/rpc_client.go:120-126`.
- New sentinel: `var ErrNoBaseFee = errors.New("RPC block has no baseFee (non-EIP-1559 block)")` per architecture §15.
- Exit code 5 per architecture §15 table.

**Acceptance criteria.**
- [x] `TestBlockBaseFee_NilBaseFee_Reject` (mock): header with nil BaseFee → `ErrNoBaseFee`.
- [x] `TestBlockBaseFee_HappyPath` (mock): non-nil → returns value.
- [x] Interface doc comment says "latest" (verified via godoc). 

---

## Issue M1.3-2 — RPC chain-ID guard fail-closed + slog logger injection

- **Points:** 2
- **Priority:** P1
- **Closes:** FR-P1-D2 (GO-033)
- **Depends on:** —

**Description.** In `internal/tx/builder.go:91-101` (`resolveRPC` chain-ID guard), fail closed on RPC error or chainID 0. Inject `*slog.Logger` so the "warn and continue" log message actually emits per architecture §6.8.

**Implementation notes.**
- File: `internal/tx/builder.go:91-101`.
- New constructor param or field for `*slog.Logger`; threaded from the calling CLI.
- "Fail closed" → return a sentinel (e.g., reuse `tx.ErrChainIDMismatch` or add `ErrChainIDZero`); operator sees a clear error rather than silent default-zero.

**Acceptance criteria.**
- [x] `TestResolveRPC_ChainIDZero_FailClosed` (mock): RPC returns 0 → error returned (no silent continue).
- [x] `TestResolveRPC_LoggerEmits_Warning` (capture logger): warn-and-continue branches actually emit a `WARN` record.
- [x] No reference to never-configured global slog default remains in this code path. 

---

## Issue M1.3-3 — Gas estimate overflow + `DepositContractAddress` direct

- **Points:** 2
- **Priority:** P1
- **Closes:** FR-P1-D3 (GO-034)
- **Depends on:** —

**Description.** Fix `estimate*6/5` overflow in `internal/tx/builder.go:139-161` to `estimate + estimate/5`. Use `cfg.NetworkParams.DepositContractAddress` directly (no hex round-trip).

**Implementation notes.**
- File: `internal/tx/builder.go:139-161`.
- Document a sanity-check ceiling (e.g., `if estimate > maxGasCap { ... }`) per architecture §6.8.

**Acceptance criteria.**
- [x] `TestGasEstimate_NearMaxUint64_NoOverflow`: `estimate = math.MaxUint64 - 1024` → safe value, no wrap.
- [x] `TestBuilder_UsesDepositContractDirectly` (read code): no `common.HexToAddress(addrStr)` round-trip.
- [x] Existing happy-path gas-estimate test still green. 

---

## Issue M1.3-4 — `errors.Is(ethereum.NotFound)` + retry transient + receipt-fail sentinel

- **Points:** 3
- **Priority:** P1
- **Closes:** FR-P1-D4 (GO-035)
- **Depends on:** M0.7-2 (sentinel exists)

**Description.** Replace `strings.Contains("not found")` with `errors.Is(err, ethereum.NotFound)` in `internal/tx/rpc_client.go:78-86`. Retry transient errors until the deadline. Map receipt-phase failures to `tx.ErrReceiptTimeout` (already exists from M0.7-2).

**Implementation notes.**
- File: `internal/tx/rpc_client.go:78-86`.
- Transient errors: network/timeout/temporary RPC errors. Use a backoff (e.g., 500 ms exp backoff up to deadline).
- `ethereum.NotFound` is the canonical "no receipt yet" indicator.

**Acceptance criteria.**
- [x] `TestReceipt_NotFound_RetriesUntilDeadline` (mock): mock returns `ethereum.NotFound` 3× then success → success returned.
- [x] `TestReceipt_NotFound_DeadlineExceeded` → `ErrReceiptTimeout` exit 5.
- [x] `TestReceipt_TransientError_Retries` (mock returns net error twice then success).

---

## Issue M1.3-5 — Hybrid `--rpc-url` wired on `run` only

- **Points:** 3
- **Priority:** P1
- **Closes:** FR-P1-D5, ADR-004 lock-in
- **Depends on:** M0.7-8a (rejection path), M1.3-2 (chain-ID guard ready)

**Description.** Per ADR-004 (project-plan Spike S3, architecture §17 Q2 closed): wire `NewEthClient` into `BuildConfig.RPC` for `run` only. `build` continues to reject `--rpc-url` per M0.7-8a. Operators get nonce/fees auto-resolved on `run`.

**Implementation notes.**
- File: `cmd/eth-deposit-tx/run.go:53-59` (config-load); thread the constructed `EthRPC` into the build phase.
- Reuse `internal/tx.resolveRPC` (retained in M0; PRD locked decision).
- If `BuildConfig` needs reshaping to accept an optional `EthRPC`, that's the split-risk listed in SUMMARY Open Scope Risks; estimate-pads as needed.
- `build --rpc-url URL` remains rejected — verify M0.7-8a's reject path still fires.

**Acceptance criteria.**
- [x] `TestRun_RPCURL_ResolvesNonceAndFees` (mock RPC): `eth-deposit-tx run --rpc-url URL` (no `--nonce`/fees) succeeds.
- [x] `TestRun_HoodiEndToEnd_RPCURL` (e2e tag, real hoodi): succeeds end-to-end.
- [x] `TestBuild_RPCURL_StillRejected`: build remains rejected (M0.7-8a regression unchanged).

---

## Issue M1.3-6 — ADR-004 hybrid `--rpc-url` decision document

- **Points:** 1
- **Priority:** P1
- **Closes:** Architecture §19 conflict #4 (FR-P1-D5 final)
- **Depends on:** M1.3-5

**Description.** Document the final hybrid `--rpc-url` decision in `docs/architecture-decisions/ADR-004.md` (or extend the existing architecture.md §20). Wire-on-`run`-only is the locked answer; record rationale + alternatives considered.

**Implementation notes.**
- The architecture.md already has ADR-004 as a section; if a separate file convention is wanted, mirror it. Otherwise update the existing §20 entry to "Accepted" status.

**Acceptance criteria.**
- [x] ADR text references M1.3-5 implementation issue.
- [x] MIGRATION.md cross-link added (M1.8 phase will reference this).
- [x] No "deferred" status remains on ADR-004.
