# Phase M1.8 — Documentation Accuracy (v1.0 Docs)

**Objective.** Close FR-P1-H — docs match the binary byte-for-byte. Lands every doc fix deferred from M0.9.

**Dependencies.** M1.1–M1.7 (every M1 phase changes CLI surface or behavior).

**Exit criteria recap (project-plan §M1.8).**
- `make doc-audit` returns 0 deltas (audited CLI contract matches behavior).
- `CHANGELOG.md` v1.0.0 entry published.
- USER-GUIDE walked end-to-end produces a successful (testnet) mainnet-shaped flow.

**Phase totals.** 4 issues, 9 points (~4.5 days single-stream).

---

## Issue M1.8-1 — USER-GUIDE.md troubleshooting layer attribution + `ErrBroadcastChainIDMismatch`

- **Points:** 2
- **Priority:** P1
- **Closes:** FR-P1-H1 (GO-068)
- **Depends on:** M1.5-9 (TestExitCodeContract is the source of truth)

**Description.** Fix `docs/USER-GUIDE.md` troubleshooting rows so each error is correctly attributed to the layer that emits it (`internal/tx.Validate` vs `Entry.Validate`). Replace misquoted `ErrChainIDMismatch` with the actual `ErrBroadcastChainIDMismatch` emitted by `send`.

**Implementation notes.**
- File: `docs/USER-GUIDE.md` troubleshooting section.
- Cross-check every sentinel against architecture §15 exit-code table.

**Acceptance criteria.**
- [ ] Every troubleshooting row's error name appears in architecture §15 exit-code table.
- [ ] `ErrBroadcastChainIDMismatch` appears verbatim.
- [ ] Maintainer review: rows are accurate (manual cross-check).

---

## Issue M1.8-2 — CHANGELOG.md v1.0.0 entry

- **Points:** 2
- **Priority:** P1
- **Closes:** v1.0 release-blocker docs
- **Depends on:** M1.1–M1.7 substantially complete

**Description.** Add v1.0.0 CHANGELOG entry covering every M1 behavior change: mainnet gate, hybrid `--rpc-url` on `run`, exit-code additions, new sentinels, env-var auto-unset, BLS Zeroize (honest framing), differential SSZ oracle, hermetic cross-validate lane, ScanDir logger signature break.

**Implementation notes.**
- Follow Keep-a-Changelog format.
- Each bullet references the FR/GO ID closing it.
- Highlight the ScanDir signature break as a non-breaking-for-CLI-callers internal change (callers within the project updated).

**Acceptance criteria.**
- [ ] v1.0.0 entry present in CHANGELOG.md.
- [ ] Every M1 PR's user-visible change is represented.

---

## Issue M1.8-3 — USER-GUIDE worked mainnet-ceremony example with `--confirm-network`

- **Points:** 2
- **Priority:** P1
- **Closes:** v1.0 release-blocker docs
- **Depends on:** M1.6-3

**Description.** Add a worked mainnet-ceremony example to `docs/USER-GUIDE.md` showing `--confirm-network=mainnet`, the local-signer-mainnet gate, and the prompt rendering from decoded RLP values.

**Implementation notes.**
- Example uses the M1.6-1/-2 flag set.
- Cross-link to MIGRATION.md (v0.2 → v1.0 section in M1.8-2).

**Acceptance criteria.**
- [ ] USER-GUIDE has a "Mainnet ceremony" section with the worked example.
- [ ] Example flags match the binary (verified by `--help` cross-check).

---

## Issue M1.8-4 — `make doc-audit` mechanism (Spike S5)

- **Points:** 3
- **Priority:** P1
- **Closes:** PRD success metric #9
- **Depends on:** M1.8-1, M1.8-3

**Description.** Per project-plan Spike S5: implement `make doc-audit`. Mechanism: parse `--help` output of both binaries; diff against documented flag tables in USER-GUIDE. Fail on any delta (flag in binary missing from docs, or vice versa).

**Implementation notes.**
- If a fancier mechanism (parse markdown tables, parse urfave/cli flag struct) is unavailable in 1 day, ship as a simple `diff` between `bin/X --help` and a committed `docs/cli-contract.txt`.
- Per SUMMARY Risk: time-box; if blocking, ship as a grep-based smoke check.

**Acceptance criteria.**
- [ ] `make doc-audit` exists and exits 0 on `develop`.
- [ ] `make doc-audit` fails when an undocumented flag is introduced (verified via test PR).
- [ ] Documented in `docs/USER-GUIDE.md` maintainer section.
