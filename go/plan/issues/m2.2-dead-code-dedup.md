# Phase M2.2 — Dead Code & Duplication Removal

**Objective.** Close FR-P2-A14, A15 — remove dead/speculative code and unify duplicated structs.

**Dependencies.** M2.1 (mechanical refactors land cleanly after hygiene).

**Exit criteria recap (project-plan §M2.2).**
- `go test ./...` green after deletions; no broken callers.
- `grep` for removed symbols returns no hits.
- `jsonEntry` defined once.

**Phase totals.** 4 issues, 7 points (~3.5 days single-stream).

---

## Issue M2.2-1 — Delete `padRight`, `tx.TxBuilder`, fake compile-time assertions, dead exports

- **Points:** 2
- **Priority:** P2
- **Closes:** FR-P2-A14
- **Depends on:** —

**Description.** Delete `padRight` (test-only), `tx.TxBuilder` consumer-less interface, fake "compile-time assertions" in `cmd/eth-deposit-tx/run.go:355-356`, `EntryFromJSON` exported function if M0 confirmed no callers, and `deposit.Request.Pubkeys` batch field if unused.

**Implementation notes.**
- Audit each symbol's callers via `grep -rn` before deletion.
- Net negative line count.

**Acceptance criteria.**
- [x] `grep -rn "padRight\|TxBuilder\|EntryFromJSON\|deposit.Request.Pubkeys"` returns no hits (or only the expected non-deleted forms).
- [x] `go test ./...` green.
- [x] `git diff --stat` shows negative net.

---

## Issue M2.2-2 — Unify `jsonEntry` between `internal/deposit/json.go` and `internal/output/output.go`

- **Points:** 2
- **Priority:** P2
- **Closes:** FR-P2-A15 (jsonEntry portion)
- **Depends on:** M2.2-1

**Description.** Per architecture §6.7: unify the duplicated `jsonEntry` struct. Pick one canonical location (architecture suggests `internal/output` since that's the schema source); the other package imports.

**Implementation notes.**
- Confirm both structs are byte-identical in JSON tag order (architecture §10.4 schema stability guarantee).
- Update all callers.

**Acceptance criteria.**
- [x] `jsonEntry` defined once.
- [x] All callers reference the canonical type.
- [x] JSON output bytes unchanged (verified via golden test).

---

## Issue M2.2-3 — Unify build flag list between `buildCommand` and `buildFlags()`

- **Points:** 1
- **Priority:** P2
- **Closes:** FR-P2-A15 (build flag duplication)
- **Depends on:** —

**Description.** Per FR-P2-A15: the `buildCommand` and `buildFlags()` list flags separately; unify into one source.

**Acceptance criteria.**
- [x] One source of truth.
- [x] `--help` output unchanged.

---

## Issue M2.2-4 — De-duplicate signer/env-var validation between `LoadSignConfig` and `LoadRunConfig`

- **Points:** 2
- **Priority:** P2
- **Closes:** FR-P2-A15 (signer/env-var dup)
- **Depends on:** —

**Description.** Extract common signer/env-var validation into a shared helper consumed by both `LoadSignConfig` and `LoadRunConfig`.

**Implementation notes.**
- File: `cmd/eth-deposit-tx/config.go` (or new helper file).
- Keep the redaction discipline from M0.8-2.

**Acceptance criteria.**
- [ ] Shared helper exists; both loaders consume it.
- [ ] Existing redaction tests still green.
- [ ] No behavioral regression.
