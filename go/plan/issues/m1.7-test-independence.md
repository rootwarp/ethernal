# Phase M1.7 — Test Independence & Fixture Hygiene

**Objective.** Close FR-P1-G — the external-authority hermetic CI lane, the read-from-disk golden fixture, and the `Key.Zeroize` documentation correction.

**Dependencies.** M0.11, M1.2 (oracle pattern reused), M1.6 (RPC-shape fixtures align with mainnet-gate matrix).

**Exit criteria recap (project-plan §M1.7).**
- `make test-cross-validate` green in CI for both `hoodi` and `mainnet` networks.
- `TestEntriesFromJSON_GoldenFile` passes reading the on-disk fixture.
- `Key.Zeroize` verified via `runtime.KeepAlive`-corrected heap-dump test.

**Phase totals.** 5 issues, 11 points (~5.5 days single-stream).

---

## Issue M1.7-1 — Dockerized cross-validate CI image (pinned `ethstaker-deposit-cli`) (split-watch)

- **Points:** 3
- **Priority:** P1
- **Closes:** FR-P1-G1 (GO-059) — image piece
- **Depends on:** M0.9-5 (ethstaker rename complete)

**Description.** Per architecture §11.3: build a Docker image with `pip install ethstaker-deposit-cli==<pinned>` baked in. Pin the image SHA-256 in the workflow. Document refresh cadence in `docs/SECURITY.md` (extending M0.1-5).

**Implementation notes.**
- File: new `Dockerfile.cross-validate`.
- Base image: minimal python slim or distroless+python.
- Pin: choose latest stable `ethstaker-deposit-cli` at time of work; record version + SHA-256.
- Image-build runs once per pin bump; results are pushed to GH container registry.
- **Split-watch:** first-time Docker work in repo (per SUMMARY Open Scope Risks); if blocking, descope CI workflow scaffolding to follow-up.

**Acceptance criteria.**
- [x] `Dockerfile.cross-validate` exists and builds locally.
- [x] Image tag pinned by SHA-256 in CI workflow.
- [x] `docs/SECURITY.md` documents refresh cadence and ethstaker-deposit-cli release tracking.
- [x] Image refuses to run if `ethstaker-deposit-cli --version` does not include "ethstaker" string (research/01 §R2).

---

## Issue M1.7-2 — `cross_validate_test.go` + `cross_validate` build tag

- **Points:** 3
- **Priority:** P1
- **Closes:** FR-P1-G1 (GO-059) — test piece
- **Depends on:** M1.7-1

**Description.** Per architecture §11.3: new Go test `cmd/eth-deposit-gen/cross_validate_test.go` with `//go:build cross_validate`. Reads `os.Getenv("DEPOSIT_CLI_BIN")`, refuses to run if binary `--version` does not contain "ethstaker", then:
1. Generates deposit data for hoodi and mainnet against a deterministic seed.
2. Pipes JSON through `ethstaker-deposit-cli verify --deposit-data <ours>`.
3. Asserts exit 0 + zero stderr.

Runs in `t.TempDir()` with sanitized `cmd.Env` (per M1.1-7 helper).

**Implementation notes.**
- File: new `cmd/eth-deposit-gen/cross_validate_test.go`.
- Build tag: `//go:build cross_validate`.
- Reuse the deterministic test material from `testdata/keys.json` (introduced in M0.10).

**Acceptance criteria.**
- [ ] Default `go test ./...` skips this test (no `cross_validate` tag).
- [ ] `go test -tags=cross_validate ./cmd/eth-deposit-gen/...` runs and passes inside the cross-validate image.
- [ ] Test refuses to run when binary is wrong CLI ("staking-deposit-cli" deprecated fork) → fails with descriptive error.

---

## Issue M1.7-3 — `.github/workflows/cross-validate.yml`

- **Points:** 2
- **Priority:** P1
- **Closes:** FR-P1-G1 (GO-059) — CI lane
- **Depends on:** M1.7-1, M1.7-2

**Description.** Add `.github/workflows/cross-validate.yml` that pulls the pinned image, runs `go test -tags=cross_validate ./cmd/eth-deposit-gen/...` for hoodi + mainnet networks.

**Implementation notes.**
- Tagged trigger or every PR per architecture §12.3 (table row `cross-validate-deposit-cli`).
- Image pulled by SHA-256 (M1.7-1).
- Sanitized env per M1.1-7.

**Acceptance criteria.**
- [ ] CI lane visible in Actions UI.
- [ ] Green for both hoodi and mainnet networks against current pin.
- [ ] `make test-cross-validate` Makefile target proxies the CI command (so local devs can repro).

---

## Issue M1.7-4 — `TestEntriesFromJSON_GoldenFile` reads on-disk fixture

- **Points:** 2
- **Priority:** P1
- **Closes:** FR-P1-G2 (GO-066)
- **Depends on:** M0.10-1

**Description.** Replace the hand-copied literal in `TestEntriesFromJSON_GoldenFile` (`internal/deposit/json_test.go`) with a real `os.ReadFile` of the committed fixture or a round-trip via the output writer.

**Implementation notes.**
- File: `internal/deposit/json_test.go`.
- Pick the on-disk approach (simpler) unless the writer round-trip is meaningfully exercised elsewhere.
- The fixture exists from M0.10-1.

**Acceptance criteria.**
- [ ] No hand-copied JSON literal remains in the test.
- [ ] Test reads from `testdata/...` and passes.
- [ ] When the fixture changes (M0.10-style refresh), the test picks up the new bytes without source-code edits.

---

## Issue M1.7-5 — `Key.Zeroize` delegates to `zeroizeBytes`; `runtime.KeepAlive` comment fix

- **Points:** 1
- **Priority:** P1
- **Closes:** FR-P1-G3 (GO-045)
- **Depends on:** —

**Description.** In `internal/keystore/key.go`, have `Key.Zeroize` delegate to the existing `zeroizeBytes` helper. Correct the `runtime.KeepAlive` comment so it accurately reflects what is kept alive (per architecture §11.4).

**Implementation notes.**
- File: `internal/keystore/key.go`.
- Verify via a `runtime.KeepAlive`-corrected heap-dump test that the Go-side bytes are wiped post-Zeroize (per PRD §3.2 metric 12).

**Acceptance criteria.**
- [ ] `Key.Zeroize` is a one-line delegate to `zeroizeBytes`.
- [ ] Comment accurately describes what `runtime.KeepAlive` guards.
- [ ] `TestKeyZeroize_HeapDumpClean` verifies wipe.
