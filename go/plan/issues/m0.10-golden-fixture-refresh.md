# Phase M0.10 — Golden-Fixture Regeneration

**Objective.** Regenerate every committed `testdata/` golden fixture under the new `--withdrawal-address` flag, exactly once. Per team-lead constraint and project-plan §M0.10, this is batched late in M0 so the regeneration captures every output-affecting change in one diff.

**Dependencies.** M0.4–M0.8 (every output-affecting M0 change merged first); M0.3 (`atomicio.WriteFileWithSuffix` filename scheme).

**Exit criteria recap (project-plan §M0.10).**
- `make test` green with regenerated fixtures.
- `make assert-no-zero-wc` lint guard green on CI; verified by a deliberately-bad PR getting blocked.
- No committed JSON contains 64 zero hex chars in any `withdrawal_credentials` field.
- New filename scheme `deposit_data-<RFC3339Nano>-<sha256[:4]>.json` present in `testdata/`.

**Phase totals.** 1 mechanical issue, 3 points (~1.5 days single-stream).

---

## Issue M0.10-1 — Refresh all golden fixtures + activate `assert-no-zero-wc` + re-enable skipped tests

- **Points:** 3
- **Priority:** P0
- **Closes:** Required for v0.2 tag (PRD §12 M0 exit criterion)
- **Depends on:** M0.4-2 (WC derivation), M0.4-9 (lint target exists), M0.4-10 (golden skip markers), M0.7-3 (FSWriter atomicio), M0.5 (read-path checks), M0.6 (RLP boundary), M0.8 (any output-affecting cleanups)

**Description.** Mechanical, single-PR refresh:
1. Add `testdata/keys.json` with a deterministic test account (BLS keystore + secp256k1 sender) per architecture §11.4.
2. Update golden-test rigs to pass `--withdrawal-address` derived from `testdata/keys.json`.
3. Run `make refresh-golden` (`REFRESH_GOLDEN=1 go test -run 'TestRefreshHoodiGolden|TestRefreshMainnetGolden'`).
4. Commit regenerated `testdata/hoodi/` and `testdata/mainnet/` in a single PR with a CHANGELOG bullet.
5. Activate `make assert-no-zero-wc` on CI by wiring into the `lint` job (extension of `.github/workflows/lint.yml`).
6. Remove every `t.Skip("golden refresh pending in M0.10")` from M0.4-10 (mechanical grep).

**Implementation notes.**
- The diff WILL be large (every committed fixture changes) — per project-plan Risk P3, reviewers should treat this as auto-generated.
- Reviewer sanity-check: spot-check one regenerated `deposit_data-*.json` shows the new filename scheme and `withdrawal_credentials` starting `0x01...`.
- The `testdata/keys.json` is test-only deterministic material; document in `testdata/README.md` (small new file) that it must NEVER be used for real deposits.
- Risk P10 mitigation: the M0.9-1 USER-GUIDE example values get a follow-up edit in the same PR (or immediate follow-up) to reference the concrete regenerated fixture bytes.

**Acceptance criteria.**
- [ ] `make test` green; no `t.Skip("golden refresh pending in M0.10")` markers remain (grep clean).
- [ ] `make assert-no-zero-wc` green on CI; deliberate test PR that reverts WC fails the gate (verified).
- [ ] `grep -rE '"withdrawal_credentials"\s*:\s*"0{64}"' testdata/` returns no hits.
- [ ] Filename scheme `deposit_data-<RFC3339Nano>-<sha256[:4]>.json` present in every fixture directory.
- [ ] `testdata/keys.json` exists with deterministic test material + README warning.
- [ ] M0.9-1 USER-GUIDE.md example bytes updated to reference the regenerated fixture (follow-up edit in same PR).
