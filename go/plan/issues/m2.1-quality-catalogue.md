# Phase M2.1 — Quality Catalogue Hygiene

**Objective.** Close the no-controversy P2 hygiene findings — doc-comment fixes, comment corrections, small file moves, dependency bumps.

**Dependencies.** M1.9 (v1.0 baseline tagged).

**Exit criteria recap (project-plan §M2.1).**
- All listed findings closed by regression-tested PRs.
- `golang.org/x/crypto` bumped; `govulncheck` re-run clean.

**Phase totals.** 11 issues, 13 points (~6.5 days single-stream).

---

## Issue M2.1-1 — `NewApp` doc + `cli_test.go:550-551` comment to exit 2

- **Points:** 1
- **Priority:** P2
- **Closes:** FR-P2-A1 (GO-039)
- **Depends on:** —

**Description.** Correct `internal/cli.NewApp` doc comment and the misleading comment at `cli_test.go:550-551` so both state the actual exit code is 2 (not 1).

**Acceptance criteria.**
- [x] Doc comment text updated.
- [x] Test comment updated.
- [x] No behavioral change.

---

## Issue M2.1-2 — Sentinel-copy in secret-leak test (or comment correction)

- **Points:** 1
- **Priority:** P2
- **Closes:** FR-P2-A2 (GO-043)
- **Depends on:** —

**Description.** Either actually copy the sentinel in the secret-leak test or correct the misleading comment. Pick whichever matches the intended test semantics.

**Acceptance criteria.**
- [x] Test source matches its comment.
- [x] Test still asserts no-leak property.

---

## Issue M2.1-3 — `scripts/e2e-testnet.sh` "NEXT STEP" → USER-GUIDE.md

- **Points:** 1
- **Priority:** P2
- **Closes:** FR-P2-A5 (GO-060)
- **Depends on:** —

**Description.** Update `scripts/e2e-testnet.sh` "NEXT STEP" message to point at `docs/USER-GUIDE.md` (not an obsolete location).

**Acceptance criteria.**
- [x] `grep "NEXT STEP" scripts/e2e-testnet.sh` matches expected text.
- [x] Script still runs.

---

## Issue M2.1-4 — `merkleize` guard `len(chunks) <= limit`

- **Points:** 1
- **Priority:** P2
- **Closes:** FR-P2-A6 (GO-061)
- **Depends on:** —

**Description.** Per architecture §6.3: replace the silent `n = max(len(chunks), limit)` floor in `internal/ssz/ssz.go:162-175` with a `len(chunks) <= limit` precondition (panic on programmer error). All five call sites already pass `limit == len(chunks)`.

**Acceptance criteria.**
- [x] `merkleize` panics on violated precondition.
- [x] All existing tests green (existing call sites satisfy invariant).
- [x] `TestMerkleize_Precondition_Panic` (using `recover()`) confirms the panic.

---

## Issue M2.1-5 — bls/ssz hygiene: alias rename, error casing, doc fixes

- **Points:** 2
- **Priority:** P2
- **Closes:** FR-P2-A7 (GO-062)
- **Depends on:** —

**Description.** Fix `Sign` doc-param name; normalize error casing; remove the `bls`-inside-`package bls` alias; prune the historical ssz package comment.

**Implementation notes.**
- File: `internal/bls/bls.go`, `internal/ssz/ssz.go` package comment.
- Each is a small mechanical fix; bundle into one PR per architecture §6.2/§6.3 cleanup.

**Acceptance criteria.**
- [x] `gofmt -l .` clean.
- [x] Errors uniformly lowercase first-word per CONVENTIONS.md.
- [x] No `bls.SomeType` reference inside `package bls`.

---

## Issue M2.1-6 — Named constant for `runtime.NumCPU() * 4`

- **Points:** 1
- **Priority:** P2
- **Closes:** FR-P2-A8 (GO-063)
- **Depends on:** —

**Description.** Extract `runtime.NumCPU() * parallelismMultiplier` into a single named constant per architecture §6.10. M2 placement.

**Acceptance criteria.**
- [x] Named constant exists with documentation comment.
- [x] No more bare `runtime.NumCPU() * 4` literals.

---

## Issue M2.1-7 — Prefix `GENERATE_FIXTURES=1` in fixture-regen docstring

- **Points:** 1
- **Priority:** P2
- **Closes:** FR-P2-A9 (GO-064)
- **Depends on:** —

**Description.** Update the fixture-regeneration docstring so the example command prefixes `GENERATE_FIXTURES=1`.

**Acceptance criteria.**
- [ ] Docstring updated.
- [ ] Matches actual fixture-regen mechanism.

---

## Issue M2.1-8 — Regen keystore test fixtures with valid 96-char BLS pubkeys

- **Points:** 2
- **Priority:** P2
- **Closes:** FR-P2-A10 (GO-065)
- **Depends on:** —

**Description.** Regenerate keystore test fixtures with valid 96-char BLS pubkeys; remove the "realistic" mislabel.

**Implementation notes.**
- The fixtures are test-only; regeneration is deterministic and committed.

**Acceptance criteria.**
- [ ] Fixtures show 96-char pubkeys (uncompressed-equivalent encoding per existing schema).
- [ ] No "realistic" mislabel comment remains.
- [ ] Existing keystore tests green.

---

## Issue M2.1-9 — APDU 6d00 comment fix + delete tautological tests

- **Points:** 1
- **Priority:** P2
- **Closes:** FR-P2-A11 (GO-067)
- **Depends on:** —

**Description.** Correct the stale APDU 6d00 comment in `internal/signer/ledger.go`; delete tautological `TestFakeSignerName`/`TestFakeSignerSign`; keep the interface compile-time assertion.

**Acceptance criteria.**
- [ ] Comment matches current geth behavior.
- [ ] Tautological tests removed; compile-time assertion remains.

---

## Issue M2.1-10 — `DEPOSIT_DATA_FILE` script comment + default consistency

- **Points:** 1
- **Priority:** P2
- **Closes:** FR-P2-A12 (GO-069)
- **Depends on:** —

**Description.** Make `DEPOSIT_DATA_FILE` default and header comment consistent in `scripts/e2e-testnet.sh`.

**Acceptance criteria.**
- [ ] Comment matches default.
- [ ] Script runs without env-var leakage.

---

## Issue M2.1-11 — `golang.org/x/crypto@latest` + `go mod tidy` + govulncheck

- **Points:** 1
- **Priority:** P2
- **Closes:** FR-P2-A13 (GO-071)
- **Depends on:** —

**Description.** `go get golang.org/x/crypto@latest && go mod tidy`. Re-run `govulncheck`.

**Acceptance criteria.**
- [ ] `go.mod` shows latest `golang.org/x/crypto`.
- [ ] `govulncheck` clean.
- [ ] All tests green.
