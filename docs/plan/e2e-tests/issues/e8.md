# E8 — P2 polish + parity audit (optional)

**Discharges:** T-15, T-16, T-18 · **Split across streams** · **Depends on:** E3, E5, E6 · **→ M7b**

Non-blocking v1 polish; each item is independently deferrable. **Optional for v1** — if descoped, the
suite still gates releases via E1–E7 (M1–M7a); T-18's parity claim is the only thing that becomes
"asserted by inspection" rather than "documented," an acceptable v1 floor. T-15/T-16 need only the existing
`send` surface and can start earlier if capacity allows; T-18 is the cross-stream join that needs
everything landed.

---

## [E8-1] `ws://` reject + SIGINT-during-estimation (T-15, T-16)

**Points:** 2 · **Stream:** B · **Depends on:** — (existing `send` surface) · **Discharges:** T-15, T-16

**Description.** Two thin binary-level assertions over the existing `send` surface, in `tests/send.rs`:
- **T-15** — `send --rpc-url ws://…` → **exit 5** (a binary-level assertion on top of the existing
  `rpc_client.rs` crate unit test, nailing the `SKILL.md` gotcha down through the CLI).
- **T-16** — SIGINT to the child mid gas/nonce estimation → **exit 4**, no broadcast (extends `send.rs`,
  DD-2 — no dedicated `signals.rs`).

**Implementation notes.** T-16: send SIGINT to the child during estimation against a deliberately slow Stub
and assert exit 4 with no broadcast. The `ws://` rejection stays a crate unit test too; T-15 only adds the
CLI-level assertion on top.

**Acceptance criteria.**
- [ ] `cargo test --test send` green.
- [ ] `send --rpc-url ws://…` → exit 5; SIGINT mid-estimation → exit 4 with no broadcast.
- [ ] `make lint && make test` green.

**Out of scope.** A dedicated `signals.rs` (DD-2); moving/expanding the crate-level `ws://` unit test.

---

## [E8-2] Verify-skill parity checklist `verify-parity.md` (T-18)

**Points:** 2 · **Cross-stream join** · **Depends on:** E3, E5, E6 · **Discharges:** T-18

**Description.** `docs/plan/e2e-tests/verify-parity.md` (new): a checklist mapping every `SKILL.md` step to
the automated test now covering it, with the carve-outs (ledger signing, cross-tool import parity, **and the
deferred interactive `new` ceremony**) explicitly marked non-automatable (DD-3). Makes G3's "verify skill
automated" a **checkable** claim and catches skill drift.

**Implementation notes.** DD-3: a checklist **doc**, not a meta-test (a meta-test risks its own drift). Map
each automatable `SKILL.md` step → a named test (the golden checks, the live-anvil pipe chain, the hybrid
RPC probes, the exit-code matrix). Mark ledger signing, cross-tool import parity, **and the interactive
`new` ceremony (deferred PTY tier this stage)** as non-automatable carve-outs. Needs the recover-v3 (E3),
goldens (E5), and live (E6) surfaces landed to map them.

**Acceptance criteria.**
- [ ] `verify-parity.md` committed; **every** automatable `SKILL.md` step maps to a named test.
- [ ] Every carve-out (ledger, cross-tool parity) explicitly marked non-automatable.
- [ ] `make lint && make test` green (doc-only change; not itself `make test`-gated).

**Out of scope.** A meta-test form (DD-3 chose the doc); automating the ledger / cross-tool carve-outs (Non-goals).
